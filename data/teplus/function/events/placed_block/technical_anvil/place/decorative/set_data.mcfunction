# @Context: runs as decorative entities for the technical anvil
# Share the same data process setup when being summoned

# Share the same current ID
scoreboard players operation @s teplus.block_id = $current teplus.block_id

# Set tags
tag @s add teplus.decorative
tag @s add teplus.techanvdec