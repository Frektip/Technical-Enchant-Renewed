# Run a function tag, so it sets correctly all the default options
function #enchantplus:set_default_config

# End the option load, so it won't overwrite on each load
# this only runs when the world is first created
scoreboard players set #new_world Enchopts 1