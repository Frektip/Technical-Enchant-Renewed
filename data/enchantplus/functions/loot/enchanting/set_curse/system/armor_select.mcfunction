# Because there is no way to disable curses (hahaha) just append the values
data modify storage teplus:loot Curses append value {id:"Fragile",lvl:1}
data modify storage teplus:loot Curses append value {id:"Sensitive",lvl:1}